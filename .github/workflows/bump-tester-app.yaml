name: Bump Tester App

on:
  workflow_dispatch:

jobs:
  bump:
    runs-on: ubuntu-latest
    steps:
    - name: Branch check
      run: |
        if [[ $BRANCH != master ]]
        then
          echo "Bump versions on master branch"
          exit 1
        fi
      env:
        BRANCH: ${{ github.ref_name }}

    - uses: actions/checkout@v3

    - name: Use Node.js 16
      uses: actions/setup-node@v3
      with:
        node-version: '16.x'
        cache: 'npm'
        cache-dependency-path: ./tester-app/package-lock.json
    
    - name: Install NPM dependencies
      working-directory: ./tester-app
      run: npm ci

    - name: Bump app version
      working-directory: ./tester-app
      run: |
        echo "NEW_VERSION=$(npm --no-git-tag-version version minor)" >> $GITHUB_ENV
    
    - name: Build
      working-directory: ./tester-app
      run: npm run build --if-present

    - name: Git configuration
      run: |
        git config user.email "<>"
        git config user.name "${GH_USERNAME}"
      env:
        GH_USERNAME: ${{ github.actor }}

    - name: Commit package.json and package-lock.json /dist
      working-directory: ./tester-app
      run: |
        git add "package.json"
        git add "package-lock.json"
        git add "dist/"
        git commit -m "Bump tester-app ${{ env.NEW_VERSION }}"
        git tag tester-app@${{ env.NEW_VERSION }}
      
    - name: Push changes to repository
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: git push origin && git push --tags